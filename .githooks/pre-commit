#!/usr/bin/env bash

set -euo pipefail

readonly HOOK_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
readonly REPO_ROOT="$(cd "${HOOK_DIR}/.." && pwd)"

"${REPO_ROOT}/build/check-attribution-policy.sh" --staged
